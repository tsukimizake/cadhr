// cadhr GUI: Elm-like 言語へのリデザイン進行中。
//
// 旧 iced エントリポイント (Model / Msg / update / view / subscription) は
// 一時的に外している。Phase 4 (Range 型 + Ranged variable + GUI 接合) で
// 新 `cadhr_lang::run_main` API ベースに作り直す。
// 旧コードは git history (master ブランチの c307596 など) に残っている。

fn main() {
    eprintln!("cadhr GUI: Elm-like 言語へのリデザイン中です。Phase 4 で復旧予定。");
}
